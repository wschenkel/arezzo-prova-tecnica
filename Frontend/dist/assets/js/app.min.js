(function(){function r(e,n,t){function o(i,f){if(!n[i]){if(!e[i]){var c="function"==typeof require&&require;if(!f&&c)return c(i,!0);if(u)return u(i,!0);var a=new Error("Cannot find module '"+i+"'");throw a.code="MODULE_NOT_FOUND",a}var p=n[i]={exports:{}};e[i][0].call(p.exports,function(r){var n=e[i][1][r];return o(n||r)},p,p.exports,r,e,n,t)}return n[i].exports}for(var u="function"==typeof require&&require,i=0;i<t.length;i++)o(t[i]);return o}return r})()({1:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _header = _interopRequireDefault(require("../src/materials/components/header/header.js"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var App = /*#__PURE__*/function () {
  function App() {
    var _this = this;

    _classCallCheck(this, App);

    new _header["default"]();
    var responseProducts = this.getProducts();
    var arrProducts = {
      products: responseProducts,
      total: responseProducts.length,
      pages: Math.ceil(responseProducts.length / 24),
      perPage: 24,
      currentPage: 1,
      previousPage: 1,
      nextPage: 2,
      initialSlice: 0,
      finalSlice: 24
    }; // Initialize ordenation by price.

    this.orderProducts('highest', arrProducts);
    this.createPagination(arrProducts); // Select

    var selOrder = document.getElementsByClassName('selOrderProducts')[0];
    selOrder.addEventListener('change', function () {
      arrProducts.currentPage = 1;
      arrProducts.nextPage = 2;
      arrProducts.previousPage = 1;
      arrProducts.initialSlice = 0;
      arrProducts.finalSlice = 24;

      _this.setAnchorActive(arrProducts);

      _this.orderProducts(selOrder.value, arrProducts);
    }); // Pagination

    document.querySelectorAll('.page-anchor').forEach(function (page) {
      page.addEventListener('click', function (event) {
        var page = event.target.getAttribute('data-page');

        _this.changePage(page, arrProducts);

        _this.setAnchorActive(arrProducts);
      });
    });
    var btnPrevious = document.getElementsByClassName('btn-previous-page')[0];
    btnPrevious.addEventListener('click', function () {
      _this.changePage(arrProducts.previousPage, arrProducts);

      _this.setAnchorActive(arrProducts);
    });
    var btnNext = document.getElementsByClassName('btn-next-page')[0];
    btnNext.addEventListener('click', function () {
      _this.changePage(arrProducts.nextPage, arrProducts);

      _this.setAnchorActive(arrProducts);
    });
  }

  _createClass(App, [{
    key: "setAnchorActive",
    value: function setAnchorActive(arrProducts) {
      document.querySelectorAll('.page-anchor').forEach(function (anchor) {
        anchor.classList.remove('active');

        if (anchor.getAttribute('data-page') == arrProducts.currentPage) {
          anchor.classList.add('active');
        }
      });

      if (arrProducts.pages === arrProducts.currentPage) {
        document.getElementsByClassName('btn-next-page')[0].classList.add('blocked');
      } else {
        document.getElementsByClassName('btn-next-page')[0].classList.remove('blocked');
      }

      if (arrProducts.currentPage === arrProducts.previousPage) {
        document.getElementsByClassName('btn-previous-page')[0].classList.add('blocked');
      } else {
        document.getElementsByClassName('btn-previous-page')[0].classList.remove('blocked');
      }
    }
  }, {
    key: "changePage",
    value: function changePage(page, arrProducts) {
      arrProducts.currentPage = parseInt(page, 10);

      if (page === 1) {
        arrProducts.initialSlice = 0;
        arrProducts.finalSlice = arrProducts.perPage;
        arrProducts.nextPage = page + 1;
        arrProducts.previousPage = page;
      } else {
        arrProducts.initialSlice = page * arrProducts.perPage - arrProducts.perPage;
        var finalSlice = page * arrProducts.perPage;
        arrProducts.finalSlice = finalSlice;

        if (finalSlice > arrProducts.total) {
          arrProducts.finalSlice = arrProducts.total;
        }

        arrProducts.previousPage = parseInt(page) - 1;

        if (parseInt(page) - 1 === 0) {
          arrProducts.previousPage = 1;
        }

        arrProducts.nextPage = parseInt(page) + 1;

        if (parseInt(page) + 1 > arrProducts.pages) {
          arrProducts.nextPage = parseInt(arrProducts.pages, 10);
        }
      }

      this.listBuilder(arrProducts);
    }
  }, {
    key: "createPagination",
    value: function createPagination(arrProducts) {
      var paginationContainer = document.getElementsByClassName('pagination-items')[0];
      var itemsBuilder = '';

      for (var i = 1; i <= arrProducts.pages; i++) {
        itemsBuilder += "\n\t\t\t<li>\n            \t<a data-page=\"".concat(i, "\" class=\"page-anchor ").concat(arrProducts.currentPage == i ? 'active' : '', "\" href=\"javascript:void(0);\">").concat(i, "</a>\n        \t</li>\n\t\t\t");
      }

      paginationContainer.innerHTML = '';
      paginationContainer.insertAdjacentHTML('beforeend', itemsBuilder);
    }
  }, {
    key: "orderProducts",
    value: function orderProducts(selValue, products) {
      switch (selValue) {
        case 'highest':
          // maior valor
          this.orderByhighest(products);
          break;

        case 'lowest':
          // menor valor
          this.orderByLowest(products);
          break;

        case 'orderly':
          // A-Z
          this.orderByOrderly(products);
          break;

        case 'inordinate':
          // Z-A
          console.log('A-Z');
          this.orderByInordinate(products);
          break;

        default:
          console.log("Valor de ordena\xE7\xE3o n\xE3o encontrado. ".concat(selValue));
      }
    }
  }, {
    key: "orderByhighest",
    value: function orderByhighest(arrProducts) {
      arrProducts.products = arrProducts.products.sort(function (a, b) {
        return parseFloat(a.price) - parseFloat(b.price);
      }).reverse();
      this.listBuilder(arrProducts);
    }
  }, {
    key: "orderByLowest",
    value: function orderByLowest(arrProducts) {
      arrProducts.products = arrProducts.products.sort(function (a, b) {
        return parseFloat(a.price) - parseFloat(b.price);
      });
      this.listBuilder(arrProducts);
    }
  }, {
    key: "orderByOrderly",
    value: function orderByOrderly(arrProducts) {
      // A-Z
      arrProducts.products = arrProducts.products.sort(function (a, b) {
        return a.name.localeCompare(b.name);
      });
      this.listBuilder(arrProducts);
    }
  }, {
    key: "orderByInordinate",
    value: function orderByInordinate(arrProducts) {
      // Z-A
      arrProducts.products = arrProducts.products.sort(function (a, b) {
        return b.name.localeCompare(a.name);
      });
      this.listBuilder(arrProducts);
    }
  }, {
    key: "getProducts",
    value: function getProducts() {
      var xhr = new XMLHttpRequest();
      xhr.open('GET', "http://".concat(window.location.host, "/api/products.json"), false);

      try {
        xhr.send();

        if (xhr.status == 200) {
          var _JSON$parse = JSON.parse(xhr.response),
              products = _JSON$parse.products;

          return products;
        } else {
          console.log("Error ".concat(xhr.status, ": ").concat(xhr.statusText));
        }
      } catch (err) {
        console.log(err);
        alert('Falha ao carregar os dados.');
      }
    }
  }, {
    key: "listBuilder",
    value: function listBuilder(arrProducts) {
      var _this2 = this;

      var productContainer = document.getElementsByClassName('list-products')[0];
      var formatedProduct = '';
      arrProducts.products.slice(arrProducts.initialSlice, arrProducts.finalSlice).map(function (product) {
        formatedProduct += _this2.productBuilder(product);
      });
      productContainer.innerHTML = '';
      productContainer.insertAdjacentHTML('beforeend', formatedProduct);
    }
  }, {
    key: "productBuilder",
    value: function productBuilder(product) {
      var template = "<li>\n\t\t\t<a class=\"box-img\" href=\"javascript:void(0);\" title=\"".concat(product.name, "\">\n\t\t\t\t<img src=\"").concat(product.image, "\" alt=\"").concat(product.name, "\"/>\n\t\t\t</a>\n\t\t\t<strong class=\"tt-product-name\">\n\t\t\t\t<a href=\"javascript:void(0);\" title=\"").concat(product.name, "\">").concat(product.name, "</a>\n\t\t\t</strong>\n\t\t\t<strong class=\"tt-product-price\">\n\t\t\t\t<a href=\"javascript:void(0);\" title=\"Product\">R$ ").concat(product.price, "</a>\n\t\t\t</strong>\n\t\t</li>");
      return template;
    }
  }]);

  return App;
}();

exports["default"] = App;
new App();

},{"../src/materials/components/header/header.js":2}],2:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var Header = function Header() {
  _classCallCheck(this, Header);

  var btnCollapse = document.getElementsByClassName('btn-collapse')[0],
      header = document.querySelector('header'),
      overlay = document.getElementsByClassName('overlay')[0],
      btnCloseCollapse = document.getElementsByClassName('btn-close-collapse')[0];
  btnCollapse.addEventListener('click', function () {
    header.classList.add('show');
    overlay.classList.add('show');
  });
  btnCloseCollapse.addEventListener('click', function () {
    header.classList.remove('show');
    overlay.classList.remove('show');
  });
};

exports["default"] = Header;

},{}]},{},[1]);
